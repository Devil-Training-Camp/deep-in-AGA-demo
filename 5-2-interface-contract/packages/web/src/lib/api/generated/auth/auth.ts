/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 智能知识问答系统 API
 * AI 知识问答系统接口契约。用户上传文档、分知识库管理,基于文档内容提问, 系统返回带依据标注(grounded)的回答,支持 SSE 流式输出与多轮对话。
 *
 * 契约来源:doc/architecture-design.md「四、API 接口契约」+ CLAUDE.md 安全/行为约束 + packages/shared/src/types(领域类型与 SSE 事件的单一事实源)。
 *
 * 鉴权:除 register/login 外,所有接口需登录态。登录态由 Supabase Auth 的 session 承载;本 schema 以 bearerAuth 抽象标注"需鉴权",不代表放弃 cookie 方案。
 *
 * OpenAPI spec version: 1.0.0
 */
import type {
  AuthCredentials,
  AuthResult
} from '../api.schemas';

import { customInstance } from '../../mutator';
import type { BodyType } from '../../mutator';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];


  /**
 * 邮箱 + 密码注册,返回登录态与用户信息。注册开放度由 Supabase Auth 配置约束 (限公司邮箱域名 / 邀请制,见架构待确认项 1),服务端强制校验。
 * @summary 注册账号
 */
export const register = (
    authCredentials: BodyType<AuthCredentials>,
 options?: SecondParameter<typeof customInstance<AuthResult>>,) => {
      return customInstance<AuthResult>(
      {url: `/api/auth/register`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: authCredentials
    },
      options);
    }
  /**
 * 邮箱 + 密码登录,签发 session。登录入口按 IP/账号限失败次数(CLAUDE.md 安全 P0)。
 * @summary 登录
 */
export const login = (
    authCredentials: BodyType<AuthCredentials>,
 options?: SecondParameter<typeof customInstance<AuthResult>>,) => {
      return customInstance<AuthResult>(
      {url: `/api/auth/login`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: authCredentials
    },
      options);
    }

type AwaitedInput<T> = PromiseLike<T> | T;

    type Awaited<O> = O extends AwaitedInput<infer T> ? T : never;

export type RegisterOpResult = NonNullable<Awaited<ReturnType<typeof register>>>
export type LoginOpResult = NonNullable<Awaited<ReturnType<typeof login>>>
