# 环境变量清单(样例)。复制为 .env.local 并填入真实值。
#
# .env.local 已 gitignore(见 .gitignore 的 .env.*),真实密钥绝不入库(CLAUDE.md 密钥保护 P0)。
# 加载方式:
#   - @kb/web(Next.js):dev/build/start 自动读取根目录 .env.local,无需额外配置。
#   - 独立 Node 脚本(@kb/db 的 migrate/healthcheck、@kb/web 的 auth:healthcheck):
#     用 node --env-file-if-exists=../../.env.local 从仓库根加载(见各 package.json)。
#
# 标注说明:
#   [必填] = 缺失则对应功能无法启动    [可选] = 有默认或仅特定功能需要
#   所属 = 消费该变量的 package(共用 = 多个 package 都依赖)


# ── 数据库 ────────────────────────────────────────────────────────────
# [必填] 所属:共用(@kb/db 直连;@kb/web 通过 @kb/db 间接依赖)
# Supabase 托管的 PostgreSQL 连接串(读写)。在 Supabase Dashboard →
# Project Settings → Database → Connection string 获取。属机密,只存服务端,绝不进前端 bundle。
# 用途:应用运行时读写、Drizzle 迁移(建表 + CREATE EXTENSION vector,见 architecture-design.md §三 ORM 注意事项)、
#       灌测试数据等写操作脚本。让 AI 做建表/迁移/写数据这类改库结构的自动化时用它。
DATABASE_URL=postgresql://postgres:password@db.your-project-ref.supabase.co:5432/postgres

# [可选] 所属:AI 工具链(.mcp.json 的 postgres MCP server)
# 只读连接串,给 postgres MCP 用——让 AI 探查表结构、查数据、验证向量检索时走这条,
# 只读权限避免误写/误删(万能钥匙 + AI 自动执行的风险见课程 5.3 讨论)。
# 拿法:在 Supabase SQL Editor 建一个只读角色(GRANT SELECT),用它的连接串;或复用 DATABASE_URL。
# 缺省时 .mcp.json 里的 ${DATABASE_URL_READONLY} 展开为空,postgres MCP 无法连库。
DATABASE_URL_READONLY=postgresql://readonly_user:password@db.your-project-ref.supabase.co:5432/postgres


# ── Supabase Auth ─────────────────────────────────────────────────────
# [必填] 所属:web(@kb/web)
# Supabase 项目 URL(形如 https://<project-ref>.supabase.co)。
SUPABASE_URL=https://your-project-ref.supabase.co

# [必填] 所属:web(@kb/web)
# Supabase publishable(anon)key。客户端可见、非机密,但本项目只在服务端用它读 session。
SUPABASE_PUBLISHABLE_KEY=your-anon-publishable-key

# [可选] 所属:web(@kb/web)
# service_role key 属机密:仅在需要绕过 RLS 的服务端任务里使用。当前代码未引用,
# 待有此类任务再启用。绝不进前端 bundle、绝不加 NEXT_PUBLIC_ 前缀(.eslintrc.json 已禁)。
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key


# ── LLM / Embedding(云 API)──────────────────────────────────────────
# 均走云 API、不自托管;业务代码只依赖 lib/llm/ 抽象层,不直接引厂商 SDK
# (architecture-design.md §六 供应商抽象层)。默认厂商是 OpenAI,下列值即 OpenAI 官方端点/模型;
# 变量名保持厂商中性,换厂商(火山方舟 Ark、Azure、Voyage 等)时只改 BASE_URL/MODEL,代码不动。
# 全部属机密,只存服务端 env,绝不进前端 bundle、绝不加 NEXT_PUBLIC_ 前缀。
#
# [必填] 对话与 query rewriting 用的 LLM。填 OpenAI API Key(sk- 开头)。
LLM_API_KEY=your-openai-api-key
# [可选] 自定义网关/兼容端点(如火山方舟 Ark、Azure、自建代理);用 OpenAI 官方端点时填下值或留空由 lib/llm/ 兜底。
LLM_BASE_URL=https://api.openai.com/v1
# [可选] 对话模型名。默认 gpt-4o-mini(低 TTFT、成本敏感档,对应 tech-selection 决策 7);要更强可换 gpt-4o。
LLM_MODEL=gpt-4o-mini

# [必填] Embedding。填 OpenAI API Key(可与 LLM 复用同一把)。必须输出 1024 维,
# 与建表的 vector(1024) + HNSW 索引对齐——维度配错只在写入时报错、定位极绕(课程 5.3 强调的坑)。
EMBEDDING_API_KEY=your-openai-api-key
# [可选] 与 LLM 同厂商同网关时可复用同一 BASE_URL;OpenAI 官方端点填下值或留空。
EMBEDDING_BASE_URL=https://api.openai.com/v1
# [可选] Embedding 模型名。默认 text-embedding-3-large,调用时须带 dimensions=1024 降到 1024 维
# (官方称仅比默认 3072 维差 1–2%,tech-selection.md 决策 7);降维参数在 lib/llm/ 里设置。
EMBEDDING_MODEL=text-embedding-3-large
#
# ── 备选:火山方舟 doubao-embedding-vision(多模态 embedding)──
# 若 OpenAI 不便充值,可换火山。注意它是「多模态」embedding,接口与 OpenAI 不兼容,
# lib/llm/ 的 embed() 需按下述差异分支处理(BASE_URL 含 volces + 模型以 ep- 开头时走多模态):
#   · 路径:POST /embeddings/multimodal(而非 /embeddings)
#   · 请求体:input 为数组 [{ type: "text", text: "..." }](而非纯字符串 input),带 dimensions: 1024
#   · 返回:data.embedding(data 是对象;而非 OpenAI 的 data[0].embedding 数组)
#   · 维度:默认 2048,带 dimensions=1024 可精确降到 1024,与 vector(1024) 对齐(已实测)
# EMBEDDING_MODEL 填火山「推理接入点」ID(ep- 开头),不是模型名;在方舟控制台开通
# doubao-embedding-vision 后创建接入点获取。
# EMBEDDING_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# EMBEDDING_MODEL=ep-xxxxxxxxxxxxx


# ── 前端可见变量(NEXT_PUBLIC_)────────────────────────────────────────
# 当前项目无任何 NEXT_PUBLIC_ 变量。所有密钥/连接串均为服务端专用,
# 禁止用 NEXT_PUBLIC_ 前缀承载机密(.eslintrc.json no-restricted-syntax 强制)。
# 日后若需向浏览器暴露非敏感常量,再在此处新增并确认其确实非机密。


# ── 本地 Mock(MSW)────────────────────────────────────────────────────
# [可选] 所属:web(@kb/web)
# 设为 true 时,开发模式下启动 MSW 浏览器 worker,拦截 /api/* 走 src/mocks 的 mock 数据,
# 无需真实后端即可联调。仅 development 生效;生产构建下该分支被 tree-shake,MSW 不进生产包。
# 判定在 src/app/layout.tsx(Server Component)里读取——故用裸名 VITE_USE_MSW 即可,
# 无需 NEXT_PUBLIC_ 前缀(该前缀是给客户端 bundle 用的,此处不需要)。
# VITE_USE_MSW=true
